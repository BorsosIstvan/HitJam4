

services:
  webserver:
    container_name: hitjam4
    image: hitjam4-app
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - /HitData:/var/www/html/HitData
    restart: always
